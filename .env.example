# energy-ml-service — copy to .env for local overrides (never commit .env).
# Read by `python -m app`; real environment variables take precedence.

# The HTTP port is fixed at 19003 in source; PORT is intentionally ignored.
# Keep 127.0.0.1: only auditor-backend calls this private service.
HOST=127.0.0.1
